import chat
from chat import commands

def on_ready(rtx):
  await channel.message.send("Successfully connected to 0.user.".format(Client))

def on_message("/c"):
  if message.content.startswith("/c" + [c])
    else }
    elif.switch([c] = +[m.a])
    
def on_message("/w","/whisper"):
  if message.content.startswith("/w","/whisper" + [a])
    else }
    elif.whispermessage([w] = +[w.a])

login.process(.env("TOKEN"))
